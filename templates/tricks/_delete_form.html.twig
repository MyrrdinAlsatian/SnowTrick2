<form
	method="post"
	class="w-full"
	action="{{ path('app_tricks_delete', {'id': trick.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">

    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ trick.id) }}">
    <button class="btn text-danger">Supprimer</button>
</form>
